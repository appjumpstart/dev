const { link, getList, unlink } = require('../lib/linker')

describe('link()', async () => {
  it('should rename the existing global package directory', () => {

  })

  it('should npm link the specified directory', () => {

  })

  it('should add the specified directory to the dev-list', () => {

  })
})

describe('getList()', () => {
  it('should return an empty list when nothing has been linked', async () => {
    expect(await getList()).toEqual([])
  })
})

describe('unlink()', () => {
  it('should', () => {

  })
})
